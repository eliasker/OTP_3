{
  "name": "server",
  "version": "1.0.0",
  "description": "test server for the socket io",
  "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
      "client": "npm run start --prefix ../kaiku_front",
      "start": "node index.js",
      "dev": "concurrently \"npm run start\" \"npm run client\""
  },
  "author": "kkn",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "node": "^13.6.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
